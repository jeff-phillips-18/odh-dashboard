{
  "name": "odh-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "concurrently \"cd backend && yarn\" \"cd frontend && yarn && yarn build\"",
    "deploy": "make deploy",
    "dev": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn start\"",
    "format": "concurrently \"cd backend && yarn format\" \"cd frontend && yarn && yarn format\"",
    "push": "make push",
    "start": "cd backend && yarn start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "undeploy": "make undeploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/opendatahub-io/odh-dashboard.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PARTHSONI95/odh-dashboard/issues"
  },
  "homepage": "https://github.com/PARTHSONI95/odh-dashboard#readme",
  "dependencies": {
    "@kubernetes/client-node": "^0.12.2",
    "concurrently": "^5.3.0",
    "fastify": "^3.5.1",
    "fastify-autoload": "^3.3.0",
    "fastify-plugin": "^2.3.4",
    "fastify-sensible": "^3.0.1",
    "fastify-static": "^3.2.1",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "fastify-cli": "^2.3.0",
    "prettier": "^2.1.2",
    "tap": "^14.10.8"
  }
}
